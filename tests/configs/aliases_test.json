{
    "database": {
        "$reference": {
            "registry": {
                "schema": "database_service",
                "version": "1.0.0"
            }
        },
        "host": "aliases.db.example.com",
        "port": 5432,
        "database_name": "aliases_db"
    },
    "user_service": {
        "$reference": {
            "registry": {
                "schema": "user_service",
                "version": "1.0.0"
            }
        },
        "database": {
            "$reference": {
                "registry": {
                    "schema": "database_service",
                    "version": "1.0.0"
                }
            },
            "host": "user.aliases.db.example.com",
            "port": 5432,
            "database_name": "user_aliases"
        },
        "cache_enabled": true
    },
    "metadata": {
        "aliases": "test"
    }
}